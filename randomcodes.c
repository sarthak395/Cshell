            // // remove last word after slash from buff
            // char *token=strtok(buff,"/");x
            // char *directories[100];
            // int i=0;
            
            // while(token!=NULL)
            // {
            //     directories[i]=token;
            //     token=strtok(NULL,"/");
            //     i++;
            // }

            // char changeddir[100]="/";
            // for(int j=0;j<i-1;j++)
            // {
            //     strcat(changeddir,directories[j]);
            //     if(j!=i-2)
            //         strcat(changeddir,"/");
            // }
            // chdir(changeddir);